NODE_ENV=development
HOST=0.0.0.0
PORT=3000
LOG_LEVEL=info

# Local development accepts identity from query/header only when enabled.
ALLOW_DEV_IDENTITY=true

# Fill after creating CloudBase environments. Never commit credentials.
TCB_ENV_ID=
TCB_SERVICE_NAME=boardgame-runtime
BGC_REPOSITORY=memory
